<h1>Edit Story</h1>

<% if @story.errors.full_messages.any? %>
    <% @story.errors.full_messages.each do |message| %>
    <div class="alert alert-danger"><%= message %></div> </br>
    <% end %>
<% end %>

<%= form_for :story, url: editstory_path do |f| %>
    <div class="container">
        <%= f.label "Description" %><br>
        <%= f.text_field :description, :value => @story.description %>
    </div>

    <div class="container">
        <%= f.label "Template" %><br>
        As <%= f.text_field :as, :value => @story.as %>,
        I want <%= f.text_field :want, :value => @story.want %>
        so that <%= f.text_field :so_that, :value => @story.so_that %>
    </div>

    <div class="container">
        <%= f.label "Acceptance criteria" %><br>
        <%= f.text_field :criteria, :value => @story.criteria %>
    </div>

    <div class="container">
        <%= f.label "Size estimate" %><br>
        <%= f.text_field :size, :value => @story.size %>
    </div>

    <%= f.hidden_field :story_column_id, :value => @story.story_column_id  %>
    <%= f.hidden_field :position, :value => @story.position %>


    <div class="container">
        <%= f.submit "Save changes!" %>
    </div>
<% end %>

<%= link_to 'Return to ' + current_board.name, current_board %>
