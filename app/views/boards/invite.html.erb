<h1>Invite User</h1>

<%= form_for :invitation, url: invite_path do |f| %>
    <div class="container">
        <%= f.label "Username*" %><br>
        <%= f.text_field :username %>
    </div>

    <div class="container">
        <%= f.submit "Invite" %>
    </div>
<% end %>
